---
import Card from "./Card/index.astro";
import { jobs } from "../lib/experience";
---

<Card
  title="Experiencia"
  body={`${jobs.length} etapa${jobs.length === 1 ? "" : "s"} profesional${jobs.length === 1 ? "" : "es"}.`}
  href="/experiencia"
  bgColor="squares-pattern"
/>